{
  "name": "@artifact-validation/adapters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --dir . test/*.test.ts"
  },
  "dependencies": {
    "@artifact-validation/contracts": "workspace:*",
    "@artifact-validation/file-store": "workspace:*",
    "serialport": "^13.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.15"
  }
}
