{
  "name": "@artifact-validation/adapters",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.config.ts --dir . test/fake-adapter.test.ts"
  },
  "dependencies": {
    "@artifact-validation/contracts": "workspace:*",
    "@artifact-validation/file-store": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.15"
  }
}
